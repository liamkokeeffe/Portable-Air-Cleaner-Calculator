import {useState} from 'react';

export function AirCleanerList(props) {
    const [airCleaners, setAirCleaners] = useState([]);
    
    function findAirCleaners() {
        // filter out air cleaners based on ranges passed from props, then sort.
    }

    return (
       // airCleaners.map() create a new list item for each
       <div></div>
    );
}